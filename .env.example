# ============================================================================
# Ahnajak Topup — Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ── MySQL Database ──────────────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ahnajak_topup

# ── JWT Authentication ───────────────────────────────────────────────────────
# Generate a random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-to-a-random-long-string
JWT_EXPIRES_IN=24h

# ── API Server ───────────────────────────────────────────────────────────────
PORT=3010
# Public URL of your server (used for G2Bulk callback URLs)
PUBLIC_BASE_URL=http://localhost:3010
# Comma-separated list of allowed CORS origins (e.g. http://localhost:8080,https://yourdomain.com)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080

# ── External API Keys ────────────────────────────────────────────────────────
ICON_API_KEY=
G2BULK_API_KEY=
# Random string for securing webhooks (generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))")
G2BULK_WEBHOOK_SECRET=

# ── Telegram ─────────────────────────────────────────────────────────────────
# Bot token for hash verification (old widget) — from @BotFather
TELEGRAM_BOT_TOKEN=
# Client Secret for OIDC code flow (mobile redirect) — from @BotFather → Bot Settings → Web Login
# Client ID is set in Admin → Site → Telegram Client ID and read from DB automatically
TELEGRAM_CLIENT_SECRET=
TELEGRAM_CHAT_ID=
# Frontend URL for OIDC redirect (e.g. https://woosaastore.com)
FRONTEND_URL=